module.exports = {
  name: 'db:seed',
  alias: ['db:s'],

  run: async toolbox => {
    // Lê os arquivos de model.
    // Adiciona linhas na tabela (por padrão 25 registros em cada) com conteúdo gerado pelo faker.js
  }
}