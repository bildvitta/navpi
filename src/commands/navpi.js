
const command = {
  name: 'navpi',
  run: async toolbox => {
    const { print } = toolbox

    print.info('Welcome to naVpi')
    // Fazer uma firula com o logo do naVpi.
  }
}

module.exports = command
